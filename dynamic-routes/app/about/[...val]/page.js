export default function Page({params}) {    
    console.log(params)
    return<div>I'm about page</div>
}

// if you want multiple parameters in the url, you can use this method, three things at once